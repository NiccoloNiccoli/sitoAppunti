title: Il sito degli appunti
description: Sito di appunti
url: /sitoAppunti

collections:
    authors:
      output: true

defaults:
  - scope:
      path: ""
    values:
      layout: "default"

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
